filebeat:
  inputs:
    -
      paths:
        - /containers/*/*.log
      document_type: syslog

output:
  logstash:
    enabled: true
    hosts:
      - elk:5044
    timeout: 15
    ssl:
      certificate_authorities:
          - /certs/logstash-beats.crt
